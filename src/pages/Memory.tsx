import { useEffect, useState } from "react";
import { invoke } from "@tauri-apps/api/core";
import { ArrowRight, Check, ExternalLink, GitBranch, Keyboard, Search, ShieldCheck, Sparkles } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import { ActivityRail } from "../components/ActivityRail";
import { ClipCard } from "../components/ClipCard";
import { useClipsStore } from "../store/clips";
import { useAppStore } from "../store/app";
import { formatDuration, formatSessionRange } from "../lib/presentation";
import type { BootstrapState, CapturePreferences, SessionSummary } from "../types";

export function Memory() {
  const { clips, loading, fetchClips, latestClipId } = useClipsStore();
  const navigate = useNavigate();
  const bootstrap = useAppStore((state) => state.bootstrap);
  const capturePreferences = useAppStore((state) => state.capturePreferences);
  const setBootstrap = useAppStore((state) => state.setBootstrap);
  const [sessions, setSessions] = useState<SessionSummary[]>([]);
  const [onboardingBusy, setOnboardingBusy] = useState(false);
  const [browserContext, setBrowserContext] = useState(false);
  const [bridgeStatus, setBridgeStatus] = useState<"idle" | "checking" | "ready" | "error">("idle");
  const [question, setQuestion] = useState("");

  useEffect(() => { void fetchClips(); }, [fetchClips]);
  useEffect(() => { invoke<SessionSummary[]>("list_sessions", { limit: 6 }).then(setSessions).catch(() => setSessions([])); }, [clips.length]);

  const resumeSession = sessions[0];
  const connectedCount = sessions.reduce((count, session) => count + Math.max(0, session.clipCount - 1), 0);
  const archiveClips = clips.slice(4, 8);

  if (loading && !clips.length) return <section className="page"><p className="eyebrow">Your memory</p><h1 className="page-title">Memory</h1><div className="skeleton-stack"><div /><div /><div /></div></section>;
  if (!clips.length && (!bootstrap || !capturePreferences)) return <section className="page"><div className="skeleton-stack"><div /><div /><div /></div></section>;
  if (!clips.length && bootstrap && !bootstrap.onboardingCompleted) return <section className="page onboarding-page"><div className="onboarding-intro"><p className="eyebrow">Welcome to Mnemo</p><h1 className="page-title">Your clipboard, with memory.</h1><p className="page-copy">Mnemo keeps useful copies on this device, then connects them into research you can actually find again.</p></div><div className="onboarding-steps"><article><span><ShieldCheck size={18} /></span><div><b>Capture starts paused</b><p>Nothing enters Mnemo until you explicitly turn capture on. Existing memories stay searchable whenever capture is paused.</p></div><Check size={16} /></article><article><span><Sparkles size={18} /></span><div><b>Local intelligence stays local</b><p>{bootstrap.embeddingStatus === "ready" ? "Your local model is ready to connect related clips." : bootstrap.embeddingStatus === "unavailable" ? "The local model could not be prepared. Keyword search still works; retry it from Settings." : "The local model will download after you continue. Search works while it loads."}</p></div><Check size={16} /></article><article><span><Keyboard size={18} /></span><div><b>Shortcuts stay under your control</b><p><kbd>Ctrl/Cmd + Shift + V</kbd> searches memory. <kbd>Ctrl/Cmd + Shift + M</kbd> pauses or resumes capture.</p></div><Check size={16} /></article></div><label className="onboarding-consent"><input type="checkbox" checked={browserContext} onChange={(event) => setBrowserContext(event.target.checked)} /><span><b>Connect browser context (optional)</b><small>When enabled, the Mnemo extension can attach the current page URL and title to a copy. It never creates clips or sends data to a remote server.</small></span></label>{browserContext && <div className="onboarding-extension"><div><strong>Install the Context Bridge</strong><p>Install the Chrome package or load the Firefox package from the private beta release, then return here.</p></div><a href="https://github.com/Diclo-fenac/Mnemo/releases/latest" target="_blank" rel="noreferrer">Open release <ExternalLink size={14} /></a><button type="button" className="quiet-button" disabled={bridgeStatus === "checking"} onClick={() => { setBridgeStatus("checking"); void invoke<boolean>("test_context_bridge").then(() => setBridgeStatus("ready")).catch(() => setBridgeStatus("error")); }}>{bridgeStatus === "checking" ? "Checking…" : bridgeStatus === "ready" ? "App bridge ready" : "Check app bridge"}</button>{bridgeStatus === "error" && <small role="alert">The local bridge is not ready. Restart Mnemo, then try again.</small>}</div>}<button className="primary-button onboarding-start" disabled={onboardingBusy} onClick={() => { setOnboardingBusy(true); void invoke<CapturePreferences>("complete_onboarding", { input: { captureEnabled: false, browserContextEnabled: browserContext } }).then((saved) => { useAppStore.getState().setCapturePreferences(saved); return invoke<BootstrapState>("get_bootstrap_state"); }).then(setBootstrap).catch(() => setOnboardingBusy(false)); }}>{onboardingBusy ? "Preparing local memory…" : "Continue with capture paused"} <ArrowRight size={15} /></button></section>;
  if (!clips.length) return <section className="page memory-empty"><p className="eyebrow">Your private memory</p><h1 className="page-title">A little context goes a long way.</h1><p className="page-copy">Copy something to begin. Mnemo keeps it on this device, groups related research, and makes it searchable when you need it.</p><div className="memory-empty-steps"><span><b>1</b> Capture is visible and controllable</span><span><b>2</b> Related clips form research sessions</span><span><b>3</b> Ask Mnemo when you need it back</span></div></section>;

  return <section className="memory-dashboard"><main className="memory-main"><header className="memory-header"><p className="eyebrow">Your private memory</p><h1 className="page-title">Pick up where you left off.</h1><p className="page-copy memory-search-hint"><Keyboard size={15} /> Press <kbd>⌘K</kbd> or <kbd>Ctrl K</kbd> to search your memory from anywhere.</p><form className="memory-ask" onSubmit={(event) => { event.preventDefault(); const query = question.trim(); if (query) navigate(`/search?q=${encodeURIComponent(query)}`); }}><Sparkles size={17} aria-hidden="true" /><input value={question} onChange={(event) => setQuestion(event.target.value)} placeholder="Ask Mnemo about something you copied…" aria-label="Ask Mnemo about something you copied" /><button type="submit" aria-label="Ask Mnemo"><Search size={17} /></button></form></header><div className="memory-grid"><section className="memory-resume"><div><p className="eyebrow">Continue</p><h2>{resumeSession?.label || "Your next research thread"}</h2><p>{resumeSession ? `${resumeSession.clipCount} captures · ${formatDuration(resumeSession.durationMs)} · ${formatSessionRange(resumeSession.startedAt, resumeSession.endedAt)}` : "Sessions appear as Mnemo observes related captures."}</p></div>{resumeSession ? <Link className="primary-button" to={`/session/${resumeSession.id}`}>Resume <ArrowRight size={15} /></Link> : <Link className="quiet-button" to="/timeline">Open timeline</Link>}</section><section className="memory-connection-card"><GitBranch size={19} /><p className="eyebrow">Connection health</p><strong>{connectedCount}</strong><span>research links forming across {sessions.length} session{sessions.length === 1 ? "" : "s"}</span><Link to="/graph">Explore graph <ArrowRight size={14} /></Link></section></div>{archiveClips.length > 0 && <section className="memory-recent"><header><div><p className="eyebrow">Earlier in your archive</p><h2>Continue exploring</h2></div><Link to="/timeline">Open archive <ArrowRight size={14} /></Link></header><div className="clips-list">{archiveClips.map((clip) => <ClipCard key={clip.id} clip={clip} density="compact" />)}</div></section>}</main><ActivityRail clips={clips} /></section>;
}
